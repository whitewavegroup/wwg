# WWG Bilingual Funnel (Vercel Ready)
- Deploy as a static site (no build). 
- Vercel will serve `index.html` and fallback all routes to SPA using `routes`.
- Edit copy in `content.json`. Set your email in `site.contact_email`.
